spring.application.name=jpaTut

# Datasource settings
spring.datasource.url=jdbc:mysql://localhost:3306/college?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=anishroot
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate / JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#run the commands from the data.sql file which we create in the resources
#spring.sql.init.mode=always
#spring.jpa.defer-datasource-initialization=true
##spring.sql.init.data-locations=classpath:data.sql